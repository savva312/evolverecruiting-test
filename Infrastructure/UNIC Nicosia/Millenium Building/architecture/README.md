# Architecture

Store building drawings and imagery here per `skills/infrastructure-building-folders/SKILL.md`.
- Use `floorplans/` for scaled plans and annotated exports (with quick open instructions for CAD/PDF sources).
- Use `photos/` for exterior/common-area shots following `skills/photo-documentation/SKILL.md` for indexing and filenames.

Add additional architecture subfolders only when you have files to place.
